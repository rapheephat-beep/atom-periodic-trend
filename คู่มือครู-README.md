# ⚛️ CER-Loop — แนวโน้มสมบัติของธาตุ (เคมี ม.4)

สื่อจำลองแบบโต้ตอบสำหรับ **ใบกิจกรรม 6 แนวโน้มสมบัติของธาตุ** ที่พานักเรียนผ่านวงจร
**ข้อกล่าวอ้าง → หลักฐาน → เหตุผล → ตรวจ → แก้** และเก็บ **ทุกร่างที่นักเรียนแก้** ไว้ให้ครูเห็นพัฒนาการรายบุคคล

| หน้า | ลิงก์ | ใช้ทำอะไร |
|---|---|---|
| **หน้านักเรียน** | https://rapheephat-beep.github.io/atom-periodic-trend/ | ทำกิจกรรมทั้ง 8 ข้อ |
| **หน้าครู** | https://rapheephat-beep.github.io/atom-periodic-trend/teacher-console.html | สร้างลิงก์ประจำห้อง · รวมผลงานทั้งห้อง |
| เวอร์ชันเดิม | https://rapheephat-beep.github.io/atom-periodic-trend/v1-original.html | สื่อรุ่นแรก เก็บไว้อ้างอิง/เทียบก่อน–หลัง |

---

## เตรียมก่อนสอน (ทำครั้งเดียว)

1. เปิด **หน้าครู** → แท็บ **1 · เตรียมรหัสห้อง**
2. ใส่ชื่อห้อง แล้ววางรายชื่อนักเรียน **บรรทัดละคน เรียงตามเลขที่** (ใส่ "1." นำหน้าหรือไม่ก็ได้ ระบบตัดให้)
3. กด **สร้างลิงก์** → ได้ลิงก์ประจำห้อง → **แจกลิงก์นี้แทนลิงก์ปกติ**

นักเรียนเปิดลิงก์แล้ว **เลือกชื่อจากรายการ** ไม่ต้องพิมพ์เอง จึงไม่มีปัญหาสะกดต่างกันหรือชื่อซ้ำตอนรวมผลงาน

> รายชื่ออยู่ในตัวลิงก์เท่านั้น ไม่ถูกส่งไปเก็บที่ใด
> ถ้าช่องทางที่ใช้ส่งลิงก์ยาวเกินไป ให้แจกลิงก์ปกติ แล้วบอกนักเรียนกด **"มีรหัสห้องจากครู"** เพื่อวางรหัส (ส่วนที่อยู่หลัง `#r=`) แทนได้

---

## ลำดับที่นักเรียนต้องทำ (สื่อบังคับลำดับไว้)

1. **ข้อกล่าวอ้าง** — ทำนายให้ครบทุกคู่ แล้วกดยืนยัน → **ล็อกถาวร แก้ไม่ได้**
   ระหว่างนี้ยังไม่บอกถูก/ผิด และตารางหลักฐานยังไม่เปิด เพื่อให้เป็นการทำนายจริง
2. **หลักฐาน** — อ่านค่าจากการ์ดแบบจำลอง กรอกลงตารางเอง กราฟขยับตามที่กรอก มีปุ่มตรวจตาราง
3. **เหตุผล** — เขียนด้วยคำพูดตัวเอง กดตรวจ ระบบบอกว่ายังขาดประเด็นไหนและควรทำอะไรต่อ **โดยไม่เฉลย** แล้วแก้ไข-ตรวจซ้ำจนพอใจ
4. **เฉลย** — เปิดได้หลังกดตรวจอย่างน้อย 1 ครั้ง
5. กด **ส่งงานให้ครู** → ได้ไฟล์ `.json` ส่งครูทางไหนก็ได้ (LINE / Classroom / อีเมล)

มีปุ่ม **พิมพ์ใบบันทึก** ออกมาเป็นเอกสารครบ CER พร้อมทุกร่าง สำหรับครูที่อยากได้กระดาษส่ง

> งานถูกเก็บไว้ในเครื่องนักเรียนอัตโนมัติ ปิดแท็บแล้วเปิดใหม่ทำต่อได้
> แต่ยังไม่ได้ส่งจนกว่าจะกดส่ง — **ย้ำให้นักเรียนกดส่งก่อนลุกจากเครื่อง** โดยเฉพาะถ้าใช้เครื่องส่วนกลาง

---

## รวมผลงาน

เปิด **หน้าครู** → แท็บ **2 · รวมผลงานนักเรียน** → ลากไฟล์ทั้งห้องมาวางทีเดียว จะได้

- **ภาพรวมรายกิจกรรม** — ทำแล้วกี่คน ระดับเฉลี่ย ถึงระดับ 3 กี่คน แก้เฉลี่ยกี่ครั้ง
- **ตารางนักเรียน × 8 กิจกรรม** — สีตามระดับ 0–3 · `↑` = ดีขึ้นจากร่างแรก · จุด = กดส่งแล้ว
- **คลิกช่อง → อ่านทุกร่าง** เรียงตามเวลา พร้อมข้อความจริง ระดับ และประเด็นที่เพิ่มจากร่างก่อน
- **บันทึกสรุปเป็น CSV** เปิดใน Excel/Sheets ได้ทันที (ใส่ BOM แล้ว ภาษาไทยไม่เพี้ยน)

ส่งไฟล์ซ้ำจากคนเดิมได้ ระบบเก็บฉบับใหม่กว่าไว้ ไม่นับซ้ำ

---

## เกณฑ์ระดับการให้เหตุผล

ระบบดู 3 สัญญาณแยกกัน แล้วสรุปเป็นระดับ

| ระดับ | ความหมาย |
|---|---|
| **0** | ยังไม่ได้ให้เหตุผล / กล่าวซ้ำข้อกล่าวอ้าง |
| **1** | อ้างข้อมูลหรือตัวเลข แต่ยังไม่มีหลักการ |
| **2** | มีหลักการทางวิทยาศาสตร์ แต่ยังเชื่อมกับหลักฐานไม่ครบ |
| **3** | มีหลักการ + เชื่อมเหตุผล (เพราะ/จึง/ทำให้) + อ้างตัวเลขจากหลักฐาน |

> ⚠️ **การตรวจนี้เป็นการจับคำสำคัญ ไม่ใช่การอ่านเข้าใจความหมาย**
> เด็กที่ใช้คำถูกแต่เรียงเหตุผลผิดอาจได้ระดับสูงเกินจริง ส่วนเด็กที่อธิบายถูกด้วยคำของตัวเองอาจได้ต่ำกว่าจริง
> ใช้เพื่อ **คัดกรองว่าควรอ่านงานของใครก่อน** และดูว่าทั้งห้องขาดแนวคิดใดร่วมกัน
> **คะแนนเก็บจริงควรมาจากที่ครูอ่านข้อความจริง** ซึ่งระบบบันทึกไว้ครบทุกร่าง

---

## กิจกรรมทั้ง 8 (ตรงตามใบกิจกรรม 6)

| ข้อ | กิจกรรม | ชุดเทียบเริ่มต้น | แถวหลักฐาน |
|---|---|---|---|
| 1.1 | ขนาดอะตอม ตามหมู่ | Li → Na → K → Rb → Fr | จำนวนระดับพลังงาน / รัศมี |
| 1.2 | ขนาดอะตอม ตามคาบ | Li → Be → B → C → N | จำนวนโปรตอน / รัศมี |
| 2.1 | ขนาดไอออนลบ | N → N⁻ → N²⁻ → N³⁻ → N⁴⁻ | จำนวน e⁻ / รัศมี |
| 2.2 | ขนาดไอออนบวก | K → K⁺ → K²⁺ → K³⁺ → K⁴⁺ | จำนวน e⁻ / รัศมี |
| 3.1 | IE₁ ตามหมู่ | Li → Na → K → Rb | จำนวนระดับพลังงาน / IE₁ |
| 3.2 | IE ลำดับที่ 1, 2, 3, … | Na: IE₁ → IE₄ | ลำดับ IE / ค่า IE |
| 4 | EA ตามหมู่ | Li → Na → K → Rb | จำนวนระดับพลังงาน / EA |
| 5 | EN ตามหมู่ | Li → Na → K → Rb | จำนวนระดับพลังงาน / EN |

ชุดเทียบ **เปลี่ยนได้** — คลิกตารางธาตุเพื่อเพิ่ม/สลับธาตุ (สูงสุด 6) หรือกด +/− ขั้นในชุดไอออน
เปลี่ยนชุดแล้วงานในข้อนั้นจะล้าง เพราะจำนวนคอลัมน์เปลี่ยน

---

## จุดสอนที่ออกแบบไว้เป็นพิเศษ

- **มาตราส่วนจริง** — อะตอมทุกตัวในแถวเดียวกันวาดด้วยมาตราส่วนเดียวกัน มีวงจุดไข่ปลาซ้อนขนาดของอีกธาตุหนึ่งให้เทียบทันที
- **N⁴⁻ ไม่เกิดขึ้นจริง** — ระบบไม่แต่งตัวเลขให้ แต่ขึ้นป้าย "ไม่เกิดขึ้นจริง" และข้อนั้นไม่นับคะแนนทำนาย พร้อมเฉลยว่าทำไม
- **จุดกระโดดของ IE** — ตรวจจับอัตโนมัติ แล้วสรุปให้ว่าธาตุนั้นมีเวเลนซ์อิเล็กตรอนกี่ตัว อยู่หมู่ใด (Na กระโดดหลัง IE₁ · Mg หลัง IE₂ · Al หลัง IE₃)
- **ไอโซอิเล็กทรอนิก** — เมื่อสองไอออนมี e⁻ เท่ากัน ระบบชี้ให้เห็นเองว่าตัวที่โปรตอนมากกว่าจะเล็กกว่า
- **EA ของ F ต่ำกว่า Cl** — กรณีสวนแนวโน้ม มีคำอธิบายไว้ในเฉลยข้อ 4

---

## แหล่งข้อมูล

รัศมีอะตอม = รัศมีเชิงคำนวณ (Clementi et al., 1963) · รัศมีไอออน = Shannon (CN = 6)
IE และ EA หน่วย kJ/mol · EN มาตรา Pauling · ค่าที่มี `≈` เป็นค่าประมาณจากแบบจำลอง (ธาตุคาบ 7 บางตัวยังไม่มีค่าวัดจริง)

ขอบเขต: คาบ 1–7 **ไม่รวมธาตุกลุ่ม f** (แลนทาไนด์และแอกทิไนด์)
